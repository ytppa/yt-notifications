const config = {
  screenPosition: "LEFT-BOTTOM", // LEFT-TOP, RIGHT-TOP, RIGHT-BOTTOM, LEFT-BOTTOM

  waitingPeriod: 2000,
  style: {
    zIndex: 1000,
    bottom: "2vw",
    left: "2vw"
  }
};

export default config;
